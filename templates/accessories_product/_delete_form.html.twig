<form method="post" action="{{ path('accessories_product_delete', {'id': accessories_product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ accessories_product.id) }}">
    <button class="btn">Delete</button>
</form>
